import React,{Fragment} from 'react';

const Home = (props)=>{
    console.log(props)
    return (
        <Fragment>
            <span>this is a beautiful home page</span>
            <br></br>
            <span>and the url is '/'</span>
        </Fragment>
    );
}

export default Home;
