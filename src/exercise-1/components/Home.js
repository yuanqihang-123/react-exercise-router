import React from 'react';

const Home = (props)=>{
    console.log(props)
    return (
        <fragment>
            <span>this is a beautiful home page</span>
            <br></br>
            <span>and the url is '/'</span>
        </fragment>
    );
}

export default Home;
